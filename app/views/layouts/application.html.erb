<!DOCTYPE html>
<html>
<head>
  <title>Projet</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href='style.css'>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <style>
    /* Additional styling for the animated title */
    .animated-title {
      animation: fadeAnimation 3s ease-in-out infinite alternate; /* New animation */
      font-family: 'Cursive'; /* Change the font family */
      color: #88C6FF; /* Change the text color */
      font-style: italic; /* Italic style */
      font-weight: bold; /* Bold style */
    }

    @keyframes fadeAnimation {
      0%, 100% {
        opacity: 1;
      }
      50% {
        opacity: 0.5;
      }
    }

    /* Add margin to the right for Log out and Profile links */
    .profile-link,
    .logout-link {
      margin-right: 20px; /* Increased margin for more spacing */
    }
  </style>
</head>
<body>
  <div class="bg-center bg-cover w-full h-screen" style="background-image: url('https://i.pinimg.com/564x/78/46/59/78465959c9d1d8fc5d54aa4ab72fbf9c.jpg')">
    <p class="notice text-white"><%= notice %></p> 
    <p class="alert text-white"><%= alert %></p>
    <div class="flex justify-between pt-2 pl-4"> <!-- Changed flex layout and added padding -->
      <h1 class="animated-title w-fit m-auto text-4xl" ><strong><em>Leanios Machine Hub</em></strong></h1>


      <% if user_signed_in? %>
        <div class="flex items-center ">
          <%= link_to "Profile", edit_user_registration_path, class: "profile-link bg-zinc-700 hover:bg-blue-600 rounded-r-lg rounded-l-lg text-white font-semibold px-4 py-2" %>
          <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "logout-link bg-zinc-700 hover:bg-blue-600 rounded-r-lg rounded-l-lg text-white font-semibold px-4 py-2" %>
        </div>
      <% else %>
        <div class="flex space-x-2">
          <%= link_to "Login", new_user_session_path, class: "text-white" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "text-white" %>
        </div>
      <% end %>
    </div>
    <%= yield %>
  </div>
</body>
</html>



